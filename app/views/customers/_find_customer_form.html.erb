<%= form_tag(block_customer_by_phone_path, method: :get) do %>
    <div class="form-group">
      <%= label_tag :search, 'Find and add by phone' %>
      <%= text_field_tag(:search, params[:search]) %>
      <%= submit_tag 'Submit' %>
    </div>
<% end %>